type Weather {
  humidity: Float
  temperature: Float
  time: String
  region: String
}

input GetWeatherInput {
  region: String
  time: String
}

type Query {
  getWeather(input: GetWeatherInput): Weather
}
